# EXIT: 0
# STDOUT: 42
# STDOUT: 2
# CLEAN: test_out.txt
fun main() -> void {
    let s = "42"
    let n = write_file("test_out.txt", bytes(s))
    let inp = read_file("test_out.txt")

    putc(inp[0])
    putc(inp[1])
    putc(10)
    putstr(itoa(n))
    putc(10)
    sys.ExitProcess(0)
}
