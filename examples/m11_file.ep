# EXIT 0
# STDOUT 42
# STDOUT 2
fun main() -> i64 {
    let s = "42";
    let n = write_file("test_out.txt", s);
    let inp = read_file("test_out.txt");

    putc(inp.data[0]);
    putc(inp.data[1]);
    putc(10);
    putstr(itoa(n));
    putc(10);
    exit(0);
}
