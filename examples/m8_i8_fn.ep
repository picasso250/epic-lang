# EXIT 0
# STDOUT HI
# STDOUT 90
fn to_upper(c: i8) -> i64 {
    if (c >= 'a') {
        if (c <= 'z') {
            return c - 32;
        }
    }
    return c;
}

fn main() -> i64 {
    putc(to_upper('h'));
    putc(to_upper('i'));
    putc(10);
    puti(to_upper('z'));
    exit(0);
}
