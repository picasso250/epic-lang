# EXIT 0
# STDOUT HI
# STDOUT 90
fun to_upper(c: i8) -> i64 {
    if c >= 'a' && c <= 'z' {
        return c - 32;
    }
    return c;
}

fun main() -> i64 {
    putc(to_upper('h'));
    putc(to_upper('i'));
    putc(10);
    putstr(itoa(to_upper('z')));
    putc(10);
    exit(0);
}
