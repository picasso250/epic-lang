# EXIT: 0

struct Token {
    kind: i64
    value: i64
}

fun main(): void {
    let tokens = new Token[3]
    push(tokens, Token {})
    push(tokens, Token {})
    push(tokens, Token {})
    tokens.data[2].kind = 42
    os.ExitProcess(0)
}