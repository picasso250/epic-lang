# EXIT: 0
# STDOUT: epic_lang
fun main(): void {
    let s = "xxepic/langyy"
    let mid = s[2:11]
    let bs = bytes(mid)
    let i = 0
    while i < len(bs) {
        if bs[i] == '/' {
            bs[i] = '_'
        }
        i = i + 1
    }
    println(str(bs))
    exit(0)
}
