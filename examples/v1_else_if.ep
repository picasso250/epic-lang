# EXIT: 0
# STDOUT: mid
fun main() -> void {
    let x = 2
    if x == 1 {
        putstr("low")
    } else if x == 2 {
        putstr("mid")
    } else {
        putstr("high")
    }
    sys.ExitProcess(0)
}
