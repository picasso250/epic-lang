# EXIT 0
# STDOUT hello
fun main() -> i64 {
    let n = write_file("test_write_file.txt", "hello");
    if n != 5 {
        exit(1);
    }
    let s = read_file("test_write_file.txt");
    putstr(s);
    exit(0);
}
