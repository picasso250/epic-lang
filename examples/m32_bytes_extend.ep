# EXIT: 0
# STDOUT: hiabab
fun main() -> void {
    let buf = new i8[1]
    extend(buf, bytes("hi"))
    putstr(str_new(buf.data, buf.len))
    let xs = bytes("ab")
    extend(xs, xs)
    putstr(str_new(xs.data, xs.len))
    os.ExitProcess(0)
}
