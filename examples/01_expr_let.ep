# EXIT: 0
# STDOUT: 42
# STDOUT: 47
# STDOUT: 4
fun main(): void {
    let x = 40 + 2
    putstr(itoa(x))
    putstr("\n")

    x = x + 5
    putstr(itoa(x))
    putstr("\n")

    let y = (10 / 3) + (10 % 3)
    putstr(itoa(y))
    putstr("\n")
}
