# EXIT: 0
# STDOUT: hello world
fun main(): void {
    let a = "hello"
    let b = " "
    let c = "world"
    let buf = new u8[]
    buf.extend(bytes(a))
    buf.extend(bytes(b))
    buf.extend(bytes(c))
    println(str(buf))
    exit(0)
}
