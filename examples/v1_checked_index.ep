# EXIT: 0
# STDOUT: Ei!
fun main(): void {
    let s = "Epic"

    let xs = new u8[4]
    push(xs, 'x')
    xs[0] = 'i'
    let out = new u8[] { s[0], xs[0], '!' }
    println(str(out))

    exit(0)
}
