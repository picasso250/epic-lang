# EXIT: 0
# STDOUT: 200

struct Point {
    x: i64
    y: i64
}

fun sum(p: Point): i64 {
    return p.x + p.y
}

fun main(): void {
    let p = Point {}
    p.x = 100
    p.y = 100
    putstr(itoa(sum(p)))
    putc(10)
    os.ExitProcess(0)
}