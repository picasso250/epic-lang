# EXIT: 0
# STDOUT: 0
# STDOUT: 0
# STDOUT: 88
struct Box {
    value: i64
}

fun main(): void {
    let s: str
    let xs: u8[]
    let b = new Box
    println(str(len(s)))
    println(str(len(xs)))
    push(xs, 'X')
    println(str(xs[0]))
    exit(b.value)
}
