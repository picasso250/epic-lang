# EXIT: 0
# STDOUT: 0
# STDOUT: 0
# STDOUT: 88
struct Box {
    value: i64
}

fun main(): void {
    let s: str
    let xs: u8[]
    let b: Box
    println(len(s))
    println(len(xs))
    push(xs, 'X')
    println(xs[0])
    os.ExitProcess(b.value)
}
