# EXIT 150

struct Token {
    kind: i64;
    value: i64;
}

fun main() -> i64 {
    let tokens = new Token[1];
    push(tokens, new Token);
    tokens.data[0].kind = 100;
    tokens.data[0].value = 50;
    return tokens.data[0].kind + tokens.data[0].value;
}
