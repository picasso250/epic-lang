# EXIT 0
# STDOUT hellobye
fun main() -> i64 {
    # string literal as expression: returns pointer
    let s: i64 = "hello";
    putstr(s);

    # strcmp
    let t: i64 = "hello";
    if strcmp(s, t) == 0 {
        putstr("bye");
    } else {
        putstr("no");
    }

    exit(0);
}
