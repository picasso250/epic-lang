# EXIT: 0
# STDOUT: hellobye
fun main() -> i64 {
    let s = "hello";
    putstr(s);

    # strcmp
    let t = "hello";
    if strcmp(s, t) == 0 {
        putstr("bye");
    } else {
        putstr("no");
    }

    exit(0);
}
