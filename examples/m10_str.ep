# EXIT: 0
# STDOUT: hellobye!
fun main() -> i64 {
    let s = "hello";
    putstr(s);

    let t = "hello";
    if s == t {
        putstr("bye");
    } else {
        putstr("no");
    }
    if s != "world" {
        putstr("!");
    }

    exit(0);
}
