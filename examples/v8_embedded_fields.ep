# EXIT: 0
# STDOUT: 42

struct Base {
    x: i64
}

struct Holder {
    Base
    y: i64
}

fun main(): i64 {
    let b = new Base { x: 40 }
    let h = new Holder { Base: b, y: 1 }
    h.x = h.x + h.y
    h.x += h.y
    println(str(h.x))
    ret 0
}
