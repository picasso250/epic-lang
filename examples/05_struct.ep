# EXIT: 0
# STDOUT: 42
# STDOUT: answer
# STDOUT: 43
struct Point {
    x: i64
    y: i64
}

struct NamedPoint {
    point: Point
    name: str
}

fun (p: Point) sum(): i64 {
    ret p.x + p.y
}

fun main(): i64 {
    let p = new Point { x: 40, y: 2 }
    println(str(p.sum()))

    let named = new NamedPoint { point: p, name: "answer" }
    println(named.name)
    named.point.x += 1
    println(str(named.point.x + named.point.y))
    ret 0
}
