# EXIT: 65

struct Data {
    flag: i8;
    value: i64;
}

fun main() -> void {
    let d = new Data;
    d.flag = 1;
    d.value = 65;
    if d.flag {
        sys.ExitProcess(d.value);
    }
    sys.ExitProcess(0);
}
