# Test self-referencing struct
struct Node {
    val: i64;
    next: &Node;
}

fun main() -> i64 {
    let a = new Node;
    a.val = 10;
    let b = new Node;
    b.val = 20;
    a.next = b;

    # a.next.val should be 20
    return a.next.val;
}
# EXIT 20
