# EXIT: 0
# STDOUT: 101
fun main(): i64 {
    let ids = new map[str]i64
    ids["main"] = 1
    ids["main"] = ids["main"] + 100

    if ids.has("main") {
        println(str(ids["main"]))
    }
    ret 0
}
