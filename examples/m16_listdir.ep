# EXIT 0
fun main() -> i64 {
    let files = listdir("examples/*.ep", 30);
    putstr("found: ");
    puti(files.len);
    putc(10);

    let i = 0;
    while i < files.len {
        putstr(files.data[i].data);
        putc(10);
        i = i + 1;
    }
    return 0;
}
