# EXIT 0
fun main() -> i64 {
    let files = listdir("examples/*.ep", 30);
    putstr("found: ");
    putstr(itoa(files.len));
    putc(10);

    let i = 0;
    while i < files.len {
        putstr(files.data[i]);
        putc(10);
        i = i + 1;
    }
    return 0;
}
