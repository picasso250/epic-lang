# EXIT 0
# STDOUT 1
# STDOUT 2
fun main() -> i64 {
    let x: i64 = 10;
    if x > 5 {
        putstr(itoa(1));
        putc(10);
    }
    if x < 5 {
        putstr(itoa(999));
        putc(10);
    } else {
        putstr(itoa(2));
        putc(10);
    }
    exit(0);
}
