# COMPILE_FAIL: str expected str, integer, bool, or u8[], got Val

struct Val {
    val: i64
}

fun run(): i64 {
    println(str(new Val { val: 3 }))
    ret 0
}

fun main(): void {
    exit(run())
}
