# COMPILE_FAIL: union Node has no common field name
struct Leaf { name: str }
struct Pair { value: i64 }
type Node = Leaf | Pair
fun main(): void {
    let n: Node = new Node(new Pair { value: 1 })
    if n.name? {
        println("unreachable")
    }
}
