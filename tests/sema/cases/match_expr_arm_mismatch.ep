# ERROR: cannot join types i64 and str
type MatchExprKind = Number | Text
fun main(): void {
    match MatchExprKind.Number {
        MatchExprKind.Number {
            1
        }
        MatchExprKind.Text {
            "text"
        }
    }
}
