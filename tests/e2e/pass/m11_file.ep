# EXIT: 0
# CLEAN: test_out.txt
fun main(): void {
    let s = "42"
    let n = write_file("test_out.txt", bytes(s))
    let inp = read_file("test_out.txt")
    if n != 2 || len(inp) != 2 || inp[0] != '4' || inp[1] != '2' {
        os.ExitProcess(1)
    }
    os.ExitProcess(0)
}
