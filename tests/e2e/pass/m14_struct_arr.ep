# EXIT: 150
type StructArrayToken {
    kind: i64
    value: i64
}
fun main(): void {
    let tokens = new StructArrayToken[]
    push(tokens, new StructArrayToken)
    tokens.data[0].kind = 100
    tokens.data[0].value = 50
    os.ExitProcess(tokens.data[0].kind + tokens.data[0].value)
}
