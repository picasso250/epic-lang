# EXIT: 0
fun run(): i64 {
    let signed: i64 = -1
    let unsigned: u64 = u64(signed)
    if unsigned != (u64(0) - u64(1)) { ret 12 }

    let signed_again: i64 = i64(unsigned)
    if signed_again != (0 - 1) { ret 13 }

    ret 0
}

fun main(): void {
    exit(run())
}
