# Test a self-referencing type with fields
type Node = {
    val: i64
    next: Node
}

fun main(): void {
    let a = new Node {}
    a.val = 10
    let b = new Node {}
    b.val = 20
    a.next = b

    # a.next.val should be 20
    exit(a.next.val)
}
# EXIT: 20
