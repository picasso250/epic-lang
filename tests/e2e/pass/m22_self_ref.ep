# Test self-referencing product
type SelfRefNode {
    val: i64
    next: SelfRefNode
}
fun main(): void {
    let a = new SelfRefNode
    a.val = 10
    let b = new SelfRefNode
    b.val = 20
    a.next = b
    # a.next.val should be 20
    os.ExitProcess(a.next.val)
}
# EXIT: 20
