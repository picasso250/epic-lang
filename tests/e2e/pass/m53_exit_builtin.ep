# EXIT: 42
fun stop_now(): i64 {
    exit(42)
}
fun choose_exit(flag: bool): i64 {
    if flag {
        ret 7
    } else {
        exit(42)
    }
}
fun main(): void {
    if len(argv) == 0 {
        let ignored_stop = stop_now()
    }
    os.ExitProcess(choose_exit(false))
}
