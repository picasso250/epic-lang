# EXIT: 0
# STDOUT: ok

fun run(): i64 {
    let xs = new i64[]
    if false && xs[0] == 1 {
        ret 1
    }
    if true || xs[0] == 1 {
        println("ok")
        ret 0
    }
    ret 2
}

fun main(): void {
    exit(run())
}
