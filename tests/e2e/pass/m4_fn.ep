# EXIT: 0
# STDOUT: 42
# STDOUT: 43
fun add(a: i64, b: i64): i64 {
    ret a + b
}

fun main(): void {
    let x = add(40, 2)
    println(str(x))
    let y = add(x, 1)
    println(str(y))
    exit(0)
}
