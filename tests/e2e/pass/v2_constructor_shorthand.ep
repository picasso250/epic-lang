# EXIT: 0
# STDOUT: 0

type Box = {
    n: i64
}

fun run(): i64 {
    # constructor shorthand: new StructName → new StructName {}
    let b = new Box
    print(str(b.n))
    ret 0
}

fun main(): void {
    exit(run())
}
