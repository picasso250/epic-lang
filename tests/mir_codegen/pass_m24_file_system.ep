# MIR-CODEGEN: read_file/write_file/system builtins.
fun main(): void {
    let s = "42"
    let n = write_file("test_out.txt", bytes(s))
    let inp = read_file("test_out.txt")
    let e = system("cmd /c exit 7")
    println(str(n + e + len(inp)))
}
