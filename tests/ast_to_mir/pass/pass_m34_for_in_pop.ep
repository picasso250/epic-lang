fun main(): i64 {
    let xs = new i64[] { 1, 2, 3 }
    let total = 0
    for i in xs {
        total += xs[i]
    }
    total += xs.pop()
    let m = new map[str]i64 { "a": 1, "b": 2 }
    for k in m {
        if m.has(k) {
            total += m[k]
        }
    }
    ret total
}
